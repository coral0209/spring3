localhost/hello/hello => 문자 보기 


1. 프로젝트 생성 - maven , groupid , artifactid 
2. pom.xml 생성 
3. web.xml 생성 
4. sprig config 
	- controller scan 설정을 (Model 은 필요 없는 상황 )
	-View Resolver (jsp , json) => 두개중 하나만 설정하면 된다. 
	
5. web.xml 에 스프링 컨테이너 관련 설정 
Model - x
Controller -> o dispatcher servlet 을 만들 때 설정파일 1개만 추가하면된다. (4번에서 만든 설정파일)

----------------------spring 설정
6. 설정된 사항을 이용하여 개발(Business logic) 
Controller class 만들기 
hello/hello url 을 처리하는 Controller 만들기 => @Controller @RequestMapping 만들기 
method 만들기  
jsp , json view  만들기  



ajax 요청 

요청을 하고 응답이 오기전까지 다른 일을 할 수 있다. 
화면이 이동하는 것은 아니다. (화면의 깜빡임이 없다. )
웹브라우저가 화면을 지웠다가 다시 보내주는것이 아니다. 



----------------------
controller test 

ajax => json 
ajax => xml 
ajax => html  


문자열이 길때 자바스크립트를 자바에 넣어서 문자열 연산을 추천하지 않는다. 

